<%= render 'shared/miniheader' %>

<div class="text-center">
  <h2><%= @contact.title %></h2>
</div>

<div class="text-center"><h2>Massage from - <%= @contact.name %></h2></div>

<div class="container">

  <%=@contact.body %>
</div>

<%= link_to 'Back to main page', root_url, class: 'btn btn-link' %>