<div class="col-md-6 col-offset-3">
<%= form_with(model: addpost, local: true) do |form| %>
  <% if addpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(addpost.errors.count, "error") %> prohibited this addpost from being saved:</h2>

      <ul>
      <% addpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :addpost_title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_field :body, id: :addpost_body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :text %>
    <%= form.text_field :text, id: :addpost_text, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Offer post', class: 'btn btn-pimary' %>
  </div>
<% end %>
</div>
